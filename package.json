{
  "name": "root",
  "private": true,
  "workspaces":{ 
    "packages": [
      "home",
      "header",
      "redux"
    ]}
  ,
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run  build",
    "clean": "lerna run --parallel clean"
  },
  "devDependencies": {
    "lerna": "^5.4.3"
  }
}
